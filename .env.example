# Copy to .env for local Postgres sync server development.
# Start the database first: bun run postgres:up
# Uses port 5433 to avoid conflicting with a system Postgres on 5432.
DATABASE_URL=postgres://melon:melon@localhost:5433/melon_sync
