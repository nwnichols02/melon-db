---
title: Overview
description: AST-first query architecture, package layers, and dependency rules.
---

Melon uses an **AST-first** query architecture: every query surface compiles into one internal representation before reaching the storage adapter.

## System overview

```mermaid
flowchart LR
  subgraph authoring [Query authoring]
    Q["@melon/db-query"]
    M["@melon/db-query-mango"]
    P["@melon/db-prisma"]
  end
  subgraph core ["@melon/db"]
    AST[QueryAst]
    DB[MelonDatabase]
  end
  subgraph adapters [Adapters]
    IM[In-memory]
    EX["Bun / Node / Expo SQLite"]
    JSI["@melon/db-sqlite-native JSI"]
  end
  Q --> AST
  M --> AST
  P --> AST
  AST --> DB
  DB --> IM
  DB --> EX
  DB --> JSI
```

*Query authoring surfaces compile to one AST; the engine routes to in-memory, Expo/Bun SQLite, or native JSI adapters.*

See [Packages](/docs/packages/melon-db) for per-package guides and [Architectural decisions](/docs/architecture/decisions) for ADRs with trade-offs.

## Package layers

| Package | Role |
|---------|------|
| `@melon/db` | Schema, AST, adapter contract, runtime engine, migrations, sync primitives |
| `@melon/db-sqlite` | SQLite adapter (Bun, Node, Expo Go, RN `/rn` export) |
| `@melon/db-sqlite-native` | Native TurboModule + C++ JSI for RN dev builds |
| `@melon/db-query` | Fluent query builder → AST |
| `@melon/db-query-mango` | Mango-style query compiler → AST |
| `@melon/db-prisma` | Prisma schema import + local client facade |
| `@melon/db-react` | React hooks and providers |
| `@melon/db-devtools` | Query / SQL / sync inspector |
| `@melon/db-testkit` | Test helpers and fixtures |
| `@melon/db-codemods` | WatermelonDB migration codemods |
| `@melon/sync` | Pull/push sync orchestrator |
| `@melon/sync-server` | HTTP reference backend (in-memory + Postgres) |

## Query pipeline

```mermaid
sequenceDiagram
  participant Author as Query author
  participant Compiler as Query compiler
  participant DB as MelonDatabase
  participant Adapter as StorageAdapter
  participant Sub as Subscribers

  Author->>Compiler: fluent / Mango / Prisma
  Compiler->>Compiler: compile to QueryAst
  Compiler->>DB: validate + plan + prepare
  DB->>Adapter: PreparedQuery
  Adapter->>Adapter: SQL or in-memory
  Adapter->>DB: rows
  DB->>Sub: observe on change
```

Steps in code:

1. Author query (fluent builder, Mango JSON, or Prisma-like args)
2. Compile to `QueryAst`
3. Validate against schema metadata
4. Plan and prepare (`PreparedQuery`)
5. Adapter encodes to SQL (SQLite) or in-memory evaluation
6. Results flow through reactive subscriptions (`MelonQueryHandle.observe`)

## Package dependencies

```mermaid
flowchart BT
  DB["@melon/db"]

  Q["@melon/db-query"]
  Mango["@melon/db-query-mango"]
  Prisma["@melon/db-prisma"]
  SQLite["@melon/db-sqlite"]

  React["@melon/db-react"]
  Devtools["@melon/db-devtools"]
  Testkit["@melon/db-testkit"]
  Codemods["@melon/db-codemods"]

  Sync["@melon/sync"]
  SyncServer["@melon/sync-server"]
  Native["db-sqlite-native"]

  Q --> DB
  Mango --> DB
  Prisma --> DB
  SQLite --> DB

  React --> DB
  Devtools --> DB
  Testkit --> DB
  Codemods --> DB

  Sync --> DB
  SyncServer --> Sync
  Native --> SQLite
```

**Rules:**

- `@melon/db` is the bottom of the stack — no other melon packages
- Query packages compile to AST only; they never touch SQLite directly
- `@melon/sync` depends only on `@melon/db`
- React bindings wire subscriptions to UI; they do not own business logic

## Writes

All mutations run inside `db.write()`. The engine serializes writers and allows reads without concurrent writers.

```mermaid
stateDiagram-v2
  [*] --> Idle
  Idle --> Writing: db.write()
  Writing --> Idle: complete
  Idle --> Reading: db.read / queries
  Reading --> Idle: complete

  note right of Writing
    No concurrent writers during Writing
  end note
```

Collection `insert`, `update`, and `delete` throw if called outside a write context.

## Subscriptions

**SQLite adapters** (`@melon/db-sqlite`) implement **`observeQuery`**: after each write, subscriptions are invalidated only when the changed row can affect the query’s WHERE clause (predicate-aware). Per-table triggers record writes to `_melon_observation_events` for future external-write polling.

**Other adapters** (in-memory, etc.) use the engine **ChangeEmitter**: any write to a collection re-fetches all active queries on that table.

The engine `observeQuery` helper prefers `adapter.observeQuery` when present. See [ADR-007](/docs/architecture/decisions#adr-007-changeemitter-fallback-vs-native-observequery).

## Related

- [Sync architecture](/docs/architecture/sync) — outbox, checkpoints, conflict policies
- [Native SQLite paths](/docs/architecture/native) — Expo Go vs dev build
- [Architectural decisions](/docs/architecture/decisions) — ADRs with pros and cons
