---
title: CRUD playground
description: In-memory tasks with reactive queries and devtools.
---

This playground uses an **in-memory** adapter so it runs in the browser without native SQLite. For SQLite, Mango, Prisma-style queries, relations, and delete CRUD on device, run **playground-rn-dev** and open the **Query demos** screen (dev builds only).

<LivePlayground />
