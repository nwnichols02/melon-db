---
title: "withRetry"
description: "@melon/sync — withRetry"
---

[**@melon/sync API Reference**](/docs/api/melon-sync)

***

# Function: withRetry()

> **withRetry**\<`T`\>(`fn`, `policy`, `options?`): `Promise`\<`T`\>

Defined in: [packages/melon-sync/src/retry.ts:57](https://github.com/nwnichols02/melon/blob/0db6fded595fdb9a7033153a12ec9b886aff6d3b/packages/melon-sync/src/retry.ts#L57)

Executes an async function with exponential backoff retries.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `Promise`\<`T`\> |
| `policy` | [`RetryPolicy`](/docs/api/melon-sync/Interface.RetryPolicy) |
| `options?` | \{ `onRetry?`: (`attempt`, `error`) => `void`; `signal?`: `AbortSignal`; \} |
| `options.onRetry?` | (`attempt`, `error`) => `void` |
| `options.signal?` | `AbortSignal` |

## Returns

`Promise`\<`T`\>
