---
title: "withRetry"
description: "API reference: withRetry"
---

[**Melon API**](index)

***

# Function: withRetry()

> **withRetry**\<`T`\>(`fn`, `policy`, `options?`): `Promise`\<`T`\>

Defined in: [packages/melon-sync/src/retry.ts:57](https://github.com/nwnichols02/melon/blob/d9796379d1a38c40c38c2d07dd61967affdbde72/packages/melon-sync/src/retry.ts#L57)

Executes an async function with exponential backoff retries.

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `Promise`\<`T`\> |
| `policy` | [`RetryPolicy`](Interface.RetryPolicy) |
| `options?` | \{ `onRetry?`: (`attempt`, `error`) => `void`; `signal?`: `AbortSignal`; \} |
| `options.onRetry?` | (`attempt`, `error`) => `void` |
| `options.signal?` | `AbortSignal` |

## Returns

`Promise`\<`T`\>
