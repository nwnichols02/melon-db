---
title: "@melon/sync-server"
description: HTTP reference sync backend (in-memory and Postgres).
---

**Status:** Shipped · **Depends on:** `@melon/sync`, `@melon/db` · **API:** [Reference](/docs/api/melon-sync-server)

## Install (alpha)

```bash
npm install @melon/sync-server@alpha
```

Or with Bun:

```bash
bun add @melon/sync-server@alpha
```

```bash
bun run sync-server
bun run sync-server:postgres
```

| Method | Path |
|--------|------|
| POST | `/sync/pull` |
| POST | `/sync/push` |

Postgres backend uses Bun SQL with `sync_meta`, `sync_tasks`, and tombstone tables. SQL migrations live in `packages/melon-sync-server/sql/`.
