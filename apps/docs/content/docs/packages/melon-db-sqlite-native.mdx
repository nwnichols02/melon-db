---
title: "@melon/db-sqlite-native"
description: Melon SQLite TurboModule for React Native development builds (Phase 20 spike).
---

**Status:** Spike (iOS) · **Expo Go:** Not supported

Native SQLite module used by [`@melon/db-sqlite/rn`](/docs/packages/melon-db-sqlite). For Expo Go, use [`@melon/db-sqlite/expo`](/docs/packages/melon-db-sqlite) instead.

## Platform support

| Platform | Status |
|----------|--------|
| iOS (dev build) | Supported |
| Android | Stub — use expo-sqlite |
| Expo Go | Not available |

## Setup

Playground uses two env files under `apps/playground-rn/env/`:

- **Expo Go:** `bun run dev:rn` — does not use this package
- **Development build:** `bun run dev:rn:dev-build` — loads `env/.env.development-build` and runs prebuild + `expo run:ios`
